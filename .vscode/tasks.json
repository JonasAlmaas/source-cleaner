{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make",
            "type": "shell",
            "command": "make",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "Make clean",
            "type": "shell",
            "command": "make clean",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
    ]
}
